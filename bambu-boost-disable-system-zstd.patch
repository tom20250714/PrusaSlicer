diff --git a/deps/Boost/Boost.cmake b/deps/Boost/Boost.cmake
--- a/deps/Boost/Boost.cmake
+++ b/deps/Boost/Boost.cmake
@@ -43,6 +43,7 @@ bambustudio_add_cmake_project(Boost
     CMAKE_ARGS
         -DBOOST_EXCLUDE_LIBRARIES:STRING=contract|fiber|numpy|wave|test
         -DBOOST_LOCALE_ENABLE_ICU:BOOL=OFF # do not link to libicu, breaks compatibility between distros
+        -DBOOST_IOSTREAMS_ENABLE_ZSTD:BOOL=OFF # avoid picking up an incompatible MSYS2 zstd in MSVC builds
         -DBUILD_TESTING:BOOL=OFF
         "${_context_abi_line}"
         "${_context_arch_line}"
